# Railway 部署配置
# Railway 会自动检测 backend/ 下的 requirements.txt 和 FastAPI 应用

[build]
builder = "NIXPACKS"
nixpacksConfigPath = "backend"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
